# ollama-cli
Use Ollama in the CLI (simple: it can read your terminal, not write files or execute commands)

./ollama-shell -m gpt-oss 

./ollama-shell -m gpt-oss -e <SOME-DOMAIN> -s https -k <YOUR-API-KEY> -p 443 -v

